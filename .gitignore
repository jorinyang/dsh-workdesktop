# 依赖与运行时
node_modules/
*.log
*.tmp
*.pid

# 本地产物 / 数据（本仓库只放插件本体，不含任何数据）
_meta/
dist/
build/
.dsh/
.env
.env.*

# ⚠️ 例外：selftest 自带的中性 fixture 也放在 `_meta/out/` 下（模拟真实产物布局），
#    否则会被上面的 `_meta/` 规则一并忽略 ⇒ 克隆体跑不起来。它们全是虚构示例，不含真实数据。
!selftest/fixtures/_meta/
!selftest/fixtures/_meta/**

# 编辑器 / 系统
.DS_Store
Thumbs.db
.idea/
.vscode/
